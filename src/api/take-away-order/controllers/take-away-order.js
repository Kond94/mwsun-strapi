'use strict';

/**
 *  take-away-order controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::take-away-order.take-away-order');
